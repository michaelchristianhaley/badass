# BADASS Agent Bootstrap

`BADASS.md` is the canonical user-owned operating constitution for this repository.

Before doing any work:

1. Read `BADASS.md` completely.
2. Read `docs/QUICK-REFERENCE.md`, `control/outline.md`, `control/inspection-map.json`, and `docs/SECTION-COMPLIANCE-MATRIX.md`.
3. Run `python3 scripts/session_gate.py --start`.
4. Run `python3 scripts/validate_repository.py --check`.
5. Stop if either command fails or if the requested work conflicts with the outline, live state, or The User's command.

This file is an integration adapter. It does not replace or weaken `BADASS.md`. Do not edit `BADASS.md` without The User's explicit authorization for that specific change.
